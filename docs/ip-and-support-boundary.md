# IP and Support Boundary

- Customer packages expose operational surface, not internal development truth.
- Support export must protect both customer IP and vendor IP.
- Forbidden: `workspace/`, `tests/`, `.env`, internal PRD/closeout, prompt/context internals.
