# Getting Started

1. Find active bundle in `RELEASE_INDEX.md`.
2. Select variant (`solo_eval` / `team_project` / `enterprise_site`).
3. Read variant `README.md` and `deployment/docs/*`.
4. Run `scripts/verify_release_bundle.sh <bundle_id>`.
5. Perform extract/install/preflight/smoke.
