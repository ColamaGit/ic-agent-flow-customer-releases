# Support Bundle Export Guide

- Define incident scope/time window before export.
- Export allowlist artifacts only.
- Apply dual-redaction (customer IP + vendor IP).
- Do not export source tree, secrets, or internal-only content.
