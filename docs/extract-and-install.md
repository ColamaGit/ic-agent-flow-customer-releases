# Extract and Install

1. Verify bundle integrity: `scripts/verify_release_bundle.sh <bundle_id>`.
2. Inspect variant artifacts (`.tar.gz`, manifest, checksums).
3. Extract package to customer working directory.
4. Follow package `deployment/docs/EXTRACT_AND_INSTALL.md`.
5. Run preflight and smoke scenario.
