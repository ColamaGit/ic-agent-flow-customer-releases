# EDA Toolchain Binding Guide

1. Prepare site-local `eda_toolchain` config.
2. Bind tool paths and version commands.
3. Run preflight (`PASS` or `PASS_WITH_WARNINGS`).
4. Store binding records for audit.
